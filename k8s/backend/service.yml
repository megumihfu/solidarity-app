apiVersion: v1
kind: Service
metadata:
  name: solidarity-backend
  namespace: solidarity

spec:
  selector:
    app: solidarity-backend

  ports:
    - port: 8080
      targetPort: 8080

  type: ClusterIP