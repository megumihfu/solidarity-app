apiVersion: v1
kind: Service
metadata:
  name: solidarity-frontend
  namespace: solidarity

spec:
  selector:
    app: solidarity-frontend

  ports:
    - port: 80
      targetPort: 80
  
  type: ClusterIP